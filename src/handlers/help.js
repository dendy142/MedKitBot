import { InlineKeyboard } from 'grammy';

const HELP_TEXT = `📖 *Помощь — Medkit Bot*

*📦 Аптечки*
Создавайте несколько аптечек (Домашняя, Дачная, В дорогу).
Делитесь аптечками с семьёй — каждый участник видит содержимое.

*💊 Лекарства*
Добавляйте лекарства с дозировкой, категорией, сроком годности и количеством.
Прикрепляйте до 5 фото и заметки.
Помечайте избранные ⭐ — они всегда вверху списка.

*📆 Приём и расписание*
Настройте курс приёма: время, дозировка, частота, длительность.
Бот напомнит о приёме и автоматически спишет остаток.

*🔍 Поиск*
Ищите лекарства по названию во всех ваших аптечках.
Просто напишите название — бот попробует найти.

*👥 Общие аптечки*
Поделитесь аптечкой по ссылке или @username.
Роли: владелец, редактор, только просмотр.

*🛒 Список покупок*
Добавляйте лекарства в список покупок.
Отмечайте купленное и пополняйте остаток.

*📊 Статистика*
Отслеживайте соблюдение курсов, стрики и историю приёмов.

*📤 Экспорт / 📥 Импорт*
Экспортируйте данные в PDF или CSV.
Импортируйте лекарства из CSV-файла.

*⚙️ Настройки*
Часовой пояс, уведомления, пороги, дайджест, отображение.

*Команды:*
/start — Главное меню
/help — Эта справка
/cancel — Отмена текущего действия`;

const HELP_KEYBOARD = new InlineKeyboard()
  .text('📦 Аптечки', 'medkits')
  .text('🛒 Покупки', 'shopping')
  .row()
  .text('◀️ Главное меню', 'main_menu');

export async function handleHelp(ctx) {
  if (ctx.callbackQuery) {
    await ctx.editMessageText(HELP_TEXT, {
      parse_mode: 'Markdown',
      reply_markup: HELP_KEYBOARD,
    });
  } else {
    await ctx.reply(HELP_TEXT, {
      parse_mode: 'Markdown',
      reply_markup: HELP_KEYBOARD,
    });
  }
}

export async function handleHelpCallback(ctx) {
  await ctx.answerCallbackQuery();
  await handleHelp(ctx);
}
